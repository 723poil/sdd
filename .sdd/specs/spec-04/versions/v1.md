# 명세 채팅 이미지/파일 첨부 지원

## 요약

- `Fits current MVP`: 기존 명세 채팅 입력을 확장해 스크린샷 이미지와 로컬 파일을 메시지와 함께 전송할 수 있게 한다.
- 오른쪽 채팅 패널에서 붙여넣은 첨부를 미리 확인하고 제거한 뒤 전송하면, assistant 응답과 함께 현재 명세 초안 갱신 흐름을 유지한다.

## 배경 / 문제

- 현재 오른쪽 채팅 패널은 `src/renderer/features/project-bootstrap/project-bootstrap-page/components/InfoSidebar.tsx`의 `textarea` 기반 입력만 제공한다.
- IPC 계약 `src/shared/ipc/project-ipc.ts`의 `SendProjectSessionMessageInput`과 도메인 계약 `src/domain/project/project-session-model.ts`의 `ProjectSessionMessage`도 `text`만 다룬다.
- 사용자는 캡처 이미지나 관련 파일을 별도 외부 도구 없이 채팅 맥락에 함께 넣고 싶지만, 지금은 텍스트로 다시 설명해야 해서 명세 협업 품질과 속도가 떨어진다.
- 현재 `src/infrastructure/spec-chat/project-spec-chat-codex-prompt.ts`도 최근 세션 메시지 텍스트만 prompt에 넣기 때문에, 시각 자료나 파일 근거가 명세 초안에 반영되지 않는다.

## 목표

- 명세 채팅에서 클립보드 이미지와 로컬 파일을 첨부해 전송할 수 있다.
- 사용자는 전송 전에 첨부 목록과 썸네일을 확인하고 개별 제거할 수 있다.
- 첨부 정보가 세션 로그와 Codex 입력에 반영돼 assistant reply와 spec draft 갱신에 함께 사용된다.
- 텍스트만 보내는 기존 흐름은 그대로 유지한다.

## 비목표

- 자동 OCR, 이미지 주석 편집, PDF 페이지 분할, Office 문서 변환.
- 분석 workspace나 참조 태그 화면으로의 범용 파일 업로드.
- 대용량 바이너리, 폴더 업로드, 클라우드 동기화, 외부 스토리지 연동.
- 채팅 외의 patch/diff 실행 단계 확장.

## 사용자 시나리오

1. 사용자는 프로젝트를 열고 `명세` 페이지에서 작업 중인 명세를 선택한다.
2. 오른쪽 `채팅` 입력창에 스크린샷을 붙여넣거나 로컬 파일을 붙여넣는다.
3. 입력창 근처에 첨부 칩 또는 썸네일이 표시되고, 사용자는 필요 없는 첨부를 제거한다.
4. 사용자는 설명 텍스트를 추가하거나 텍스트 없이 첨부만 전송한다.
5. 메시지가 전송되면 채팅 타임라인에 텍스트와 첨부 요약이 함께 표시된다.
6. assistant는 첨부 맥락을 반영한 답변을 반환하고, 현재 명세 초안도 함께 갱신된다.
7. 지원하지 않는 형식 또는 용량 초과면 전송 전에 이유가 짧게 표시되고, 다른 첨부와 텍스트 입력은 유지된다.

## 기능 요구사항

- 오른쪽 채팅 composer는 붙여넣기 이벤트를 처리해 이미지와 파일 첨부를 수집할 수 있어야 한다.
- 1차 지원 대상은 스크린샷 이미지(`png`, `jpg`/`jpeg`, `webp`)와 로컬 텍스트/코드 계열 파일(`txt`, `md`, `json`, `yaml`/`yml`, `toml`, `ts`, `tsx`, `js`, `jsx`, `css`, `html`, `java`, `kt`, `php`, `py`)로 제한한다.
- 메시지는 `텍스트만`, `텍스트+첨부`, `첨부만` 세 경우를 모두 허용한다.
- 첨부는 한 메시지에 여러 개를 둘 수 있고, 전송 전 개별 삭제가 가능해야 한다.
- 채팅 타임라인은 각 메시지의 첨부 개수와 이름을 보여주고, 이미지는 작은 미리보기를 제공한다.
- 이미지 첨부는 프로젝트 내부 `.sdd/sessions/<session-id>/...` 아래에 저장 가능한 형태로 정규화한 뒤 메시지와 연결해야 한다.
- 파일 첨부는 원본 파일명을 보존하는 메타데이터와 앱이 참조할 실제 저장 경로 또는 스냅샷 경로를 남겨야 한다.
- 세션 메시지 저장 계약은 첨부 manifest를 포함하도록 확장해야 하며, 기존 첨부 없는 세션 로그도 계속 읽을 수 있어야 한다.
- `SendProjectSessionMessageInput`, renderer IPC facade, main IPC handler, use case, session repository, spec-chat adapter가 첨부 배열을 함께 전달해야 한다.
- `src/infrastructure/spec-chat/project-spec-chat-codex-prompt.ts`는 최근 메시지 텍스트뿐 아니라 현재 turn의 첨부 정보를 명시적으로 prompt 입력에 포함해야 한다.
- Codex 응답이 실패해도 사용자 메시지와 첨부 저장은 보존해야 한다.
- 텍스트 전용 전송 UX와 단축 흐름은 회귀 없이 유지해야 한다.

## 비기능 요구사항

- 기본 visible copy는 최소화하고, 형식 또는 용량 설명은 툴팁이나 짧은 오류 문구로만 노출한다.
- renderer는 파일 시스템에 직접 접근하지 않고 typed IPC를 통해 첨부 저장과 전송을 요청해야 한다.
- Codex 실행은 계속 `main process`의 `codex exec` 경로를 사용한다.
- 첨부 저장 관련 JSON 계약은 `schemaVersion`을 가져야 하고, mutable metadata는 기존 `revision` 또는 `updatedAt` 규칙과 충돌하지 않아야 한다.
- 세션 메시지 로그는 append-only 특성을 유지해야 하며, 첨부 추가 때문에 기존 메시지를 재작성하지 않는다.
- 프로젝트가 읽기 전용이면 현재와 동일하게 채팅 자체를 허용하지 않으며, 성공한 첨부 저장을 가장해선 안 된다.
- 초기 제한값 초안은 메시지당 최대 5개, 파일당 최대 20MB, 총합 최대 30MB로 둔다.
- 지원하지 않는 형식 또는 크기 오류는 전송 전에 판별하고, 실패 이유는 사용자 행동 기준으로 짧게 표시한다.

## 참조 태그

- 기존 참조 태그: `.sdd/analysis/manual-reference-tags.json` 파일이 현재 저장소에 없어 확인된 수동 태그 없음
- 새 제안 태그: `spec-chat`
- 새 제안 태그: `chat-attachment`
- 새 제안 태그: `session-storage`
- 새 제안 태그: `ipc-typed-bridge`

## 영향도 분석

### 관련 파일 / 모듈

- renderer 채팅 UI: `src/renderer/features/project-bootstrap/project-bootstrap-page/components/InfoSidebar.tsx`
- renderer 채팅 상태와 전송 흐름: `src/renderer/features/project-bootstrap/project-bootstrap-page/use-project-bootstrap-workbench.workflow.ts`
- renderer 타입과 상태 모델: `src/renderer/features/project-bootstrap/project-bootstrap-page/project-bootstrap-page.types.ts`
- shared IPC 계약: `src/shared/ipc/project-ipc.ts`, `src/shared/ipc/project-ipc-api.ts`
- main IPC 조립: `src/main/ipc/project-ipc-registration.ts`
- application 포트와 유스케이스: `src/application/project/project.ports.ts`, `src/application/project/send-project-session-message.use-case.ts`
- domain 세션 계약: `src/domain/project/project-session-model.ts`
- session 저장소: `src/infrastructure/sdd/fs-project-session.repository.ts`, `src/infrastructure/sdd/fs-project-session-documents.ts`
- spec chat prompt와 adapter: `src/infrastructure/spec-chat/project-spec-chat-codex-prompt.ts`, `src/infrastructure/spec-chat/node-project-spec-chat.adapter.ts`
- 분석 근거: `.sdd/analysis/context.json`에 위 레이어 경로들이 현재 앱 소스로 나열돼 있고, `.sdd/analysis/file-index.json`은 `src/main/ipc/project-ipc-registration.ts`가 `src/application/project/send-project-session-message.use-case.ts`와 `src/infrastructure/spec-chat/node-project-spec-chat.adapter.ts`를 직접 import하는 흐름을 보여준다.

### 예상 변경

- renderer는 `textarea` 단독 입력에서 `텍스트 + 첨부 목록` 상태로 확장된다.
- IPC 입력과 출력 계약은 메시지 본문 외에 첨부 manifest를 주고받도록 바뀐다.
- session message schema가 확장되며, 저장소는 첨부 파일 저장과 `messages.jsonl` append를 함께 처리해야 한다.
- spec chat prompt는 현재 대화 이력 외에 첨부 입력 path 또는 메타를 Codex가 읽을 수 있는 구조로 전달해야 한다.
- 문서 동기화 대상은 `docs/codex-spec-workflow/04-storage-format.md`, `docs/codex-spec-workflow/08-codex-cli-integration.md`가 우선이며, 저장 계약이나 UI 규칙 표현이 바뀌면 `docs/codex-spec-workflow/06-design-patterns.md`, `docs/codex-spec-workflow/07-engineering-conventions.md`도 같이 확인해야 한다.

### 리스크 / 확인 필요

- `codex exec`가 이미지와 파일 첨부를 어떤 형식으로 가장 안정적으로 읽는지 현재 코드만으로는 확정되지 않는다. 경로 입력만으로 충분한지, 별도 입력 계약이 필요한지 검증이 필요하다.
- macOS clipboard에서 Finder 파일 복사와 스크린샷 붙여넣기를 동일 UX로 다룰 수 있는지 Electron 이벤트 검증이 필요하다.
- 이미지 원본을 프로젝트 내부 `.sdd/`에 복사 저장할지, 원본 파일 경로만 참조할지 결정이 필요하다. 재진입과 이동 가능성을 고려하면 사본 저장이 더 안전하다.
- 현재 `tests/` 아래에는 session attachment 계약을 검증하는 테스트가 보이지 않으므로 domain, use case, repository, IPC 순서로 신규 테스트가 필요하다.

## 사용 스킬

- `sdd-product-mvp`: 이 기능이 현재 MVP의 명세 채팅 입력 확장인지 확인한다.
- `sdd-implementation-guardrails`: renderer, preload, main, application, infrastructure 경계를 지키면서 첨부 흐름을 분리한다.
- `sdd-storage-contracts`: `.sdd/sessions/*` 첨부 저장 위치, 메시지 `schemaVersion`, append-only 규칙을 설계한다.
- `sdd-ui-disclosure`: 오른쪽 채팅 패널의 첨부 도움말과 오류 문구를 과밀하지 않게 조정한다.
- `sdd-agent-xml-prompt`: `src/infrastructure/spec-chat/project-spec-chat-codex-prompt.ts`의 XML 입력 계약에 첨부 컨텍스트를 추가할 때 사용한다.
- `sdd-codex-cli`: `codex exec` 기반 첨부 전달 방식과 main process 실행 제약을 점검한다.
- `sdd-engineering-conventions`: 타입 확장, 파일 분리, 테스트 우선순위를 점검한다.

## 수용 기준

- [ ] 명세 페이지의 오른쪽 채팅 패널에서 스크린샷 이미지 붙여넣기 후 첨부 목록이 보인다.
- [ ] 지원 파일을 붙여넣거나 첨부했을 때 전송 전 제거가 가능하다.
- [ ] 텍스트 없이 첨부만으로도 메시지 전송이 가능하다.
- [ ] 전송된 사용자 메시지는 세션 타임라인에 첨부 요약과 함께 보인다.
- [ ] 세션 저장소가 첨부 manifest를 포함한 메시지를 재로드할 수 있다.
- [ ] assistant 응답 생성 시 첨부 맥락이 prompt 입력에 반영되고, 기존처럼 spec draft, title, summary, reply가 반환된다.
- [ ] Codex 응답 실패 시에도 사용자 메시지와 첨부 저장은 남고, 실패 이유가 짧게 표시된다.
- [ ] 기존 텍스트 전용 채팅 전송과 spec draft 저장 동작이 회귀하지 않는다.

## 오픈 질문

- 1차 범위를 `붙여넣기 전용`으로 자를지, 같은 첨부 모델로 `파일 선택`까지 함께 넣을지?
- 이미지와 파일 첨부를 `.sdd/sessions/<session-id>/...`에 항상 사본 저장할지, 일부는 원본 경로 참조를 허용할지?
- 지원 파일 형식에서 PDF를 1차에 포함할지, 텍스트와 코드, 이미지까지만 먼저 갈지?
- `codex exec` 입력에서 이미지 첨부를 path만으로 충분히 처리할 수 있는지, 추가 prompt 계약 보강이 필요한지?